server:
  port: 8085

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auditdb
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
